export class CountryDetailed {
  constructor(public flag: string, public name: string, public capital: string, public area: number, public population: number, public languages: Object[], public alpha3Code: string) {
  }
}
